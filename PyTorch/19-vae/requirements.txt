# Pure PyTorch + torchvision pipeline (no PyG/torch_scatter/torch_sparse).
# torchvision provides InceptionV3 for FID computation in utils.gan_utils.
# MLflow 3.10 auto-creates mlflow.db on first experiment write (see README).

torch==2.5.1+cu121
torchvision==0.20.1+cu121
numpy==2.3.5
pandas==2.3.3
matplotlib==3.10.8
scipy==1.17.0
mlflow==3.10.0