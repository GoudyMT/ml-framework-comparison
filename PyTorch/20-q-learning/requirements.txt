# PyTorch Q-Learning #20 - V1 Tabular -> V5 PER on Taxi / CartPole / LunarLander
# Windows .venv (Python 3.12.2, RTX 4090, CUDA 12.1)
#
# gymnasium[classic-control,box2d]: classic-control covers Taxi-v4 +
#   CartPole-v1; box2d covers LunarLander-v3 physics. Extras pull in
#   pygame-ce, box2d-py, farama-notifications, and swig as transitive deps.
# MLflow 3.10 auto-creates mlflow.db on first experiment write (see README).
# No torchvision: Q-Learning has no pretrained backbones / image inputs.
# No pandas / scipy: comparison tables are pure-Python aligned prints; no
#   distributional stats beyond numpy.

torch==2.5.1+cu121
gymnasium[classic-control,box2d]==1.3.0
numpy==2.3.5
matplotlib==3.10.8
mlflow==3.10.0