# TF GNN pipeline uses torch + torch_geometric ONLY for data loading (read
# preprocessed Cora Planetoid cache + ogbn-arxiv). No torch_scatter / torch_sparse
# / pyg_lib needed — training runs entirely in TensorFlow on WSL2 GPU.

tensorflow==2.21.0
torch==2.5.1
torch_geometric==2.7.0
ogb==1.3.6
numpy==2.4.3
pandas==3.0.2
matplotlib==3.10.8
scipy==1.17.1
