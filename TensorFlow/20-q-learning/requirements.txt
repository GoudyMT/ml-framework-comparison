# TensorFlow Q-Learning #20 - V1 Tabular (Taxi-v4) + V2 DQN (CartPole-v1) only.
# WSL2 Ubuntu + tf-gpu-venv (Python 3.12, CUDA 12.x via WSL2 GPU passthrough).
#
# gymnasium[classic-control] covers CartPole-v1; Taxi-v4 needs no extras.
# No box2d extra - LunarLander-v3 is V4/V5 only and not used by TF side.
# No mlflow - deployment winners live on PT side; TF side is cross-framework
#   comparison only (see data/results/q_learning_*.json).

tensorflow==2.21.0
gymnasium[classic-control]==1.3.0
numpy==2.4.3
matplotlib==3.10.8